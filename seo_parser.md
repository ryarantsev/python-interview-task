# Тестовое задание: SEO Analyzer Service

Привет! Это тестовое задание на позицию Intern Python Backend Developer.
Твоя задача — создать сервис для анализа веб-страниц.
Ориентировочное время выполнения: **4-5 часов**.

## 🛠 Технические требования

1.  **Стек**: Python 3.11+, **FastAPI**.
2.  **Библиотеки**: Можно использовать `BeautifulSoup`, `lxml` или `Playwright` (будет плюсом).
3.  **Репозиторий**: Код должен быть выложен на GitHub.
4.  **Запуск**: Обязательно наличие `Dockerfile`.
5.  **Тесты**: Написать 2-3 unit-теста (используй `mock` для имитации внешних сайтов).

## 📝 Задача: "Web Page Analyzer"

Нужно написать сервис, который скачивает HTML страницы по URL и извлекает из них ключевые метаданные.

### Функционал

**1. POST /api/analyze** — анализ страницы.
* **Вход:** `{"url": "https://habr.com"}`
* **Логика:**
    * Сервис делает HTTP-запрос на указанный URL.
    * Парсит HTML и извлекает:
        * Текст заголовка (`<title>`).
        * Количество тегов `h1`.
        * Мета-описание (`<meta name="description">`).
    * Возвращает JSON с результатами.

**2. Обработка ошибок**:
* Если URL недоступен, тайм-аут или 404 ошибка — сервис должен вернуть в JSON понятное описание ошибки, а HTTP статус ответа должен соответствовать проблеме (например, 400 Bad Request при невалидном URL).

### (*) Задания со звездочкой (по желанию, будет плюсом)
* **Playwright / Selenium**: Реализовать парсинг через headless-браузер, чтобы получать заголовки даже с динамических сайтов (SPA на React/Vue), где контент грузится через JS.
* **Кеширование**: Если этот URL уже анализировали в последние 5 минут — возвращать результат из памяти, не делая повторный запрос.

## 📚 Полезные ссылки для выполнения
* [Beautiful Soup Documentation](https://www.crummy.com/software/BeautifulSoup/bs4/doc/)
* [Playwright for Python](https://playwright.dev/python/docs/intro)
* [Unittest.mock — мокирование в тестах](https://docs.python.org/3/library/unittest.mock.html)
* [FastAPI: Pydantic Models](https://fastapi.tiangolo.com/tutorial/body/)

## ✅ Чек-лист перед отправкой
- [ ] Парсер корректно извлекает Title и H1.
- [ ] Сервис не падает при ошибке соединения с целевым сайтом.
- [ ] В тестах используются моки (тесты работают без интернета).
- [ ] Dockerfile включает установку всех зависимостей.
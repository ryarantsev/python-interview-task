# Тестовое задание: AI Assistant Service

Привет! Это тестовое задание на позицию Intern Python Backend Developer.
Твоя задача — создать прокси-сервис для работы с AI (OpenAI или имитация).
Ориентировочное время выполнения: **4-5 часов**.

## 🛠 Технические требования

1.  **Стек**: Python 3.11+, **FastAPI**, openaai-python.
2.  **Репозиторий**: Код должен быть выложен на GitHub.
3.  **Запуск**: Обязательно наличие `Dockerfile`.
4.  **Конфигурация**: API-ключи и настройки должны читаться из переменных окружения (Environment Variables).
5.  **Тесты**: Написать 2-3 unit-теста (`pytest`).

## 📝 Задача: "AI Proxy Wrapper"

Нужно написать сервис, который принимает вопрос от пользователя, отправляет его в LLM (Large Language Model) и возвращает ответ.

### Функционал

**1. POST /api/ask** — задать вопрос.
* **Входные данные (JSON):** `{"question": "Как дела?"}`
* **Логика:**
    * Сервис должен проверить наличие переменной окружения `OPENAI_API_KEY`.
    * **Важно:** Реализовать **"Mock-режим"**. Если ключа нет или переменная `MOCK_MODE=true`, сервис не делает реальный запрос, а имитирует задержку (1-2 сек) и возвращает заглушку: *"Это тестовый ответ, так как включен Mock-режим"*.
    * Если режим реальный — сделать запрос к API (Допускается любая LLM со свободным доступом) и вернуть текст.

**2. GET /api/history** — история запросов.
* Возвращает последние 10 вопросов и ответов, которые прошли через сервис.

**3. Обработка ошибок**:
* Если внешний сервис (LLM провайдер) недоступен или вернул ошибку, твой сервис должен вернуть корректный HTTP код (502/503/429), а не упасть с 500 Internal Server Error.

### \* Задания со звездочкой (по желанию, будет плюсом)
* **Стриминг**: Реализовать эндпоинт, который отдает ответ потоком (StreamingResponse), чтобы слова появлялись постепенно.
* **Защита**: Добавить простой механизм проверки токена (Header `Authorization`), чтобы сервисом не могли пользоваться все подряд.

## 📚 Полезные ссылки для выполнения
* [Управление переменными окружения (python-dotenv)](https://pypi.org/project/python-dotenv/)
* [OpenAI Python Library](https://github.com/openai/openai-python)
* [FastAPI: Handling Errors (Обработка ошибок)](https://fastapi.tiangolo.com/tutorial/handling-errors/)
* [HTTPX (Асинхронный HTTP клиент)](https://www.python-httpx.org/)
* [Вариант свободного LLM провайдера](https://openrouter.ai/)

## ✅ Чек-лист перед отправкой
- [ ] Сервис запускается без реального ключа OpenAI (в Mock-режиме).
- [ ] Переменные окружения не захардкожены в коде.
- [ ] История запросов сохраняется (в памяти или БД).
- [ ] Dockerfile рабочий.
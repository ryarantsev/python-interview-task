# Тестовое задание: URL Shortener

Привет! Это тестовое задание на позицию Intern Python Backend Developer.
Твоя задача — создать сервис сокращения ссылок со сбором статистики.
Ориентировочное время выполнения: **4-5 часов**.

## 🛠 Технические требования

1.  **Стек**: Python 3.11+, **FastAPI**.
2.  **Репозиторий**: Код должен быть выложен на GitHub.
3.  **Запуск**: Обязательно наличие `Dockerfile`.
4.  **Логирование**: Логирование всех переходов по ссылкам.
5.  **Тесты**: Написать 2-3 unit-теста (`pytest`).

## 📝 Задача: "Short Link Service"

Нужно написать сервис, который превращает длинные ссылки в короткие и перенаправляет пользователей.

### Функционал

**1. POST /api/shorten** — сократить ссылку.
* **Вход:** `{"url": "https://google.com"}`
* **Выход:** `{"short_url": "http://localhost:8000/AbCd12", "id": "AbCd12"}`
* **Логика:** Генерировать уникальный ID (4-6 символов) и сохранять связь с оригинальным URL.

**2. GET /{short_id}** — переход по ссылке.
* **Логика:**
    * Найти оригинальный URL по ID.
    * Вернуть ответ с HTTP-кодом **307 Temporary Redirect** (или 301/302, обоснуй выбор) и заголовком `Location`.
    * **Асинхронно** увеличить счетчик переходов для этой ссылки (клиент не должен ждать записи в БД/память).

**3. GET /api/stats/{short_id}** — статистика.
* Возвращает информацию о ссылке и количество переходов.

### * Задания со звездочкой (по желанию, будет плюсом)
* **Custom Alias**: Разрешить пользователю самому придумывать короткий ID (например, `/my-blog`). Обработать случай, если такой ID уже занят (вернуть 409 Conflict).
* **TTL (Time To Live)**: Удалять ссылки, которые не использовались больше 24 часов.

## 📚 Полезные ссылки для выполнения
* [HTTP Redirections (MDN Web Docs)](https://developer.mozilla.org/en-US/docs/Web/HTTP/Redirections)
* [FastAPI: Custom Response (RedirectResponse)](https://fastapi.tiangolo.com/advanced/custom-response/#redirectresponse)
* [Shortuuid library (Генерация ID)](https://pypi.org/project/shortuuid/)
* [Concurrency in FastAPI](https://fastapi.tiangolo.com/async/)

## ✅ Чек-лист перед отправкой
- [ ] Сервис корректно делает редирект в браузере.
- [ ] Счетчик переходов обновляется.
- [ ] Код 404 возвращается, если ссылка не найдена.
- [ ] Docker контейнер запускается.
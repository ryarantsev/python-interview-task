# Тестовое задание на позицию Python Backend Developer (Intern)

Привет! Перед тобой описание общих требований к тестовому заданию.
Твоя задача — выбрать один из предложенных вариантов заданий (в отдельных файлах) и реализовать его, соблюдая правила, описанные ниже.

Мы подобрали для тебя сразу несколько вариантов заданий - выбери одно, в котором, по твоему мнению, ты сможешь максимально проявить себя.

# Варианты заданий
1. **Тема: Асинхронность и фоновые задачи -** [Сервис уведомлений](notification_service.md)
2. **Тема: Интеграция с внешними API и работа с окружением -** [AI-ассистент](ai_assistant.md)
3. **Тема: HTTP стандарты, редиректы и System Design -** [Сокращатель ссылок](url_shortener.md)
4. **Тема: Парсинг, HTML и Web Scraping -** [Парсер](seo_parser.md)

## 🛠 Общие технические требования

Эти требования применимы к **любому** выбранному варианту задания.
### 1. Язык и технологии
- **Язык**: Python 3.11 или выше.
- **Фреймворк для API:** FastAPI.
- **Зависимости**: Все зависимости должны быть зафиксированы в `requirements.txt` (или `pyproject.toml`, если используешь Poetry).
- **Системы управления зависимостями:** uv, poetry, venv.

### 2. Архитектура и структура
- Код должен быть организован логично. Мы не требуем строгого следования Clean Architecture, но всё не должно лежать в одном файле `main.py`.
- Рекомендуем разделять логику:
    - `models/` или `schemas/` — Pydantic модели.
    - `endpoints/` или `routers/` — Обработчики HTTP запросов.
    - `services/` — Бизнес-логика.

### 3. Конфигурация
Приложение должно конфигурироваться через **переменные окружения** (Environment Variables).
- Рекомендуем использовать `pydantic-settings` или `python-dotenv`.
- Хардкод настроек (например, токенов, портов, хостов) внутри кода **запрещен**.
- Приложение должно поддерживать параметры (минимум эти, если в задании не указано иное):
    - `LOG_LEVEL` (по умолчанию `INFO`).
    - `APP_HOST` / `APP_PORT`.

### 4. Логирование

- Приложение должно писать логи в `STDOUT`.
- **Формат**: Читаемый текст или JSON.
- Каждая запись лога должна содержать:
    1. Уровень логирования (INFO, ERROR, DEBUG).
    2. Временную метку (Timestamp).
    3. Сообщение.
- **Request Logging**: Сервис должен логировать входящие запросы (Method, Path, Status Code) и время их обработки.

### 5. Документирование
- Код должен быть понятным. Используй **Type Hints** (аннотации типов) везде.
- Все экспортируемые функции и классы должны иметь **Docstrings**.
- API должно быть описано автоматически через **Swagger/OpenAPI** (встроено в FastAPI). Убедись, что модели имеют `description` и примеры (`example`).

### 6. Тестирование
- Фреймворк: `pytest`.
- Покрытие тестами: Напиши **минимум 3 теста**, проверяющих основные сценарии (успешный запрос, ошибка валидации, логическая ошибка).
- Для внешних вызовов (API, БД) используй **Mocks** (библиотека `unittest.mock` или фикстуры pytest). Тесты не должны зависеть от наличия интернета.

### 7. Контейнеризация
- В корне проекта должен быть **Dockerfile**.
- Команда `docker build -t my-app . && docker run -p 8000:8000 my-app` должна успешно запускать сервис.

### 8. \* Graceful Shutdown (Необязательно, но будет плюсом)
- Сервис должен корректно завершать работу при получении сигналов `SIGINT` и `SIGTERM`.
- Закрывать соединения, завершать фоновые задачи перед выходом.

---

## 📦 Сдача работы и оформление

1. **Репозиторий**: Создай публичный репозиторий на GitHub.
2. **История изменений**:
    - Коммиты должны быть атомарными (один коммит — одна логическая задача).
    - Сообщения коммитов должны быть на английском (или русском), понятными и информативными (например: `feat: add notification endpoint`, `fix: logger format`).

3. **README.md**: В корне твоего репозитория должен быть файл с инструкцией:
    - Как запустить проект локально.
    - Как запустить через Docker.
    - Как запустить тесты.
    - Краткое описание выбранного задания и принятых архитектурных решений.

## 🚀 Процесс выполнения
1. Внимательно прочитай общие требования (этот файл).
2. Выберите **один** вариант задания из предложенных.
3. Реализуй сервис, соблюдая сроки (ориентир 3 календарных дня, оценочные затраты на выполнение каждого задания 3-5 часов).
4. Проверь себя по чек-листу в конце задания.
5. Отправь ссылку на репозиторий рекрутеру/нанимающему менеджеру.

---

**Удачи! Мы ценим чистый код, простоту решений и внимание к деталям.**

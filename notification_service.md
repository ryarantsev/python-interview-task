# Тестовое задание: Сервис уведомлений (Notification Service)

Привет! Это тестовое задание на позицию Intern Python Backend Developer.
Твоя задача — разработать микросервис для управления рассылками.
Ориентировочное время выполнения: **4-5 часов**.

## 🛠 Технические требования

1.  **Стек**: Python 3.11+, **FastAPI**.
2.  **Репозиторий**: Код должен быть выложен на GitHub.
3.  **Запуск**: Обязательно наличие `Dockerfile` для запуска сервиса одной командой.
4.  **Логирование**: Сервис должен писать логи в STDOUT (уровень INFO/DEBUG).
5.  **Тесты**: Написать 2-3 unit-теста (используй `pytest`).
6.  **Кодстайл**: Соблюдение PEP8.

## 📝 Задача: "Smart Notification System"

Нужно написать API, который принимает заявки на отправку уведомлений (Email/Telegram) и имитирует процесс отправки с задержкой, не блокируя клиента. (Функционал отправки - mock, иммитация действия).

### Функционал

**1. POST /api/notifications** — создать уведомление.
* **Входные данные (JSON):**
    ```json
    {
      "user_id": 123,
      "message": "Ваш код: 1111",
      "type": "telegram" 
    }
    ```
    * Поле `type` может быть `telegram` или `email`.
* **Логика:**
    1.  Сервис сохраняет уведомление в памяти (или БД) со статусом `pending`.
    2.  Сервис возвращает ответ клиенту **сразу** (201 Created), не дожидаясь реальной отправки.
    3.  В фоне запускается процесс отправки:
        * Если `email`: "спим" 1 секунду (`asyncio.sleep(1)`), затем меняем статус на `sent`.
        * Если `telegram`: "спим" 0.2 секунды, затем меняем статус на `sent`.

**2. GET /api/notifications/{user_id}** — получить историю.
* Возвращает список уведомлений пользователя.
* **Параметры фильтрации**: Добавить query-параметр `status` (например, `?status=sent`), чтобы получить только отправленные сообщения.

### (\*) Задания со звездочкой (по желанию, будет плюсом)
* **Retry механизм**: Имитировать ошибку отправки с вероятностью 10%. В случае ошибки сервис должен попробовать отправить снова (до 3 раз), прежде чем поставить статус `failed`.
* **Docker Compose**: Подключить реальную БД (PostgreSQL или SQLite) вместо хранения в памяти.

## 📚 Полезные ссылки для выполнения
* [FastAPI: Background Tasks (Фоновые задачи)](https://fastapi.tiangolo.com/tutorial/background-tasks/)
* [Python Asyncio: основы](https://docs.python.org/3/library/asyncio-task.html)
* [Pydantic: Валидация данных](https://docs.pydantic.dev/latest/)
* [Pytest: как писать тесты для API](https://fastapi.tiangolo.com/tutorial/testing/)

## ✅ Чек-лист перед отправкой
- [ ] Docker-контейнер собирается и запускается.
- [ ] API работает асинхронно (клиент не ждет секунду при отправке Email).
- [ ] Работает фильтрация в GET запросе.
- [ ] Написаны тесты.